
	В калькулятре используются константы:
		pi = 3.14159265, e = 2.71828

	Используются следующие функции:
		cos(), sin(), tan(), asin(), acos(), atan() - для тригонометрических выражений (при вычислениях используются радианы)
		sqrt() - извлечение квадратного корня из неотрицательного числа
		abs() - получение абсолютного значения числа
		ln() - логарифм по основанию e
		log() - логарифм по основанию 10
		ceil(a) - округление а до наибольшего целого, но не больше чем а
		floor(a) - округление а до наименьшего целого, но не меньше чем а
		fmod(a, b) - вычисление остатка от a/b

		Для возведения числа в степень используется оператор ^

	Выражение может быть как числом, так и переменной. Для работы с переменными используются следующие конструкции: 
var, [имя переменной], =, выражение - для инициальзации новой переменной, 
[имя переменной], =, выражение - для изменения переменной

	Для изменения режима работы программы войдите в настройки программы (s).

	В данной версии калкулятора есть возможность работать с обычными и римскими числами. 
При работе с римскими числами используются только арифметические действия.

	Ключевое слово [from_file] позволяет вводить данные из файла. 
Причем вывод при вводе из файла настраивается отдельно от вывода из консоли 
(возможна ситуация, когда при вводе из консоли вывод будет производиться только в файл, а при вводе из файла - в консоль 
и наоборот - при вводе из консоли вывод будет только в консоль, а при вводе из файла - только в файл). 
Причем при вводе из файла вывод в файл будет производиться в любом случае.

	В программе ведется лог ошибок (выводится в error_log.log)
